---
import Layout from "../../../layouts/Layout.astro";
import BlogListByFilter from "../../../components/organisms/blogListByFilter.astro";
import { getCategoryList, getAllContents } from "../../../libs/microcms";
import type { Blog, Category } from "../../../types";
import type { GetStaticPathsOptions } from "astro";
import { getCategoryAndTagCounts } from "../../../utils/countingUtils";

export async function getStaticPaths({ paginate }: GetStaticPathsOptions) {
	const categoryResponse = await getCategoryList();
	const categories = categoryResponse.contents;
	const allBlogsResponse = await getAllContents<Blog>("blogs");
	const allBlogs = allBlogsResponse.contents;

	return categories.flatMap((category) => {
		const blogsInThisCategory = allBlogs.filter((blog) => blog.category.id === category.id);

		return paginate(blogsInThisCategory, {
			pageSize: 10,
			params: { slug: category.slug },
			props: {
				category: category,
				totalCount: blogsInThisCategory.length,
			},
		});
	});
}

const { page, category, totalCount } = Astro.props as {
	page: { data: Blog[] };
	category: Category;
	totalCount: number;
};

const blogs = page.data as Blog[];
const { categoryCounts, tagCounts } = await getCategoryAndTagCounts();
---

<Layout title={`${category.name} の記事一覧｜みやたブログ`} breadcrumb={true} category={category}>
	<BlogListByFilter
		filterType="category"
		filterLabel={category.name}
		blogs={blogs}
		page={page}
		totalCount={totalCount}
		categoryCounts={categoryCounts}
		tagCounts={tagCounts}
		filterSlug={category.slug}
	/>
</Layout>
